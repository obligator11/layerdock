# Step 1: app shell only
pywebview>=5.1

# Added in later steps, listed here so requirements.txt stays the
# single source of truth as we go:
# PyMuPDF>=1.24      (Step 2: PDF parsing)
# python-docx>=1.1   (Step 3: DOCX generation)
# pytesseract>=0.3   (Step 4: OCR — also requires the Tesseract binary)
# Pillow>=10.0       (Step 4: image handling for OCR)
# pyinstaller>=6.0   (Step 8: packaging)
